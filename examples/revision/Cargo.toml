[package]
name = "canary-agent-revision-example"
version = "0.1.0"
edition = "2021"

[dependencies]
canary-agent-revision = { path = "../../canary-agent-revision" }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
